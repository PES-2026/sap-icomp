services:
  db:
    image: postgres:latest
    container_name: sap-icomp-postgres-database-test
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT}:${DB_PORT}"
    volumes:
      - postgres_datatest:/var/lib/postgresql/data

  backend:
    image: sap-icomp-backend-test:latest
    container_name: sap-icomp-backend-test
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT}:${BACKEND_PORT}"
    depends_on:
      - db

volumes:
  postgres_datatest:
